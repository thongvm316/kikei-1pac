export const BUTTON_SIZES = Object.freeze({
  lg: 'lg',
  md: 'md',
  sm: 'sm'
})

export const BUTTON_VARIANTS = Object.freeze({
  primary: 'primary',
  danger: 'danger',
  outlinePrimary: 'outline-primary',
  outlineDanger: 'outline-danger',
  default: 'default'
})

export const BUTTON_NATIVE_TYPES = Object.freeze({
  button: 'button',
  submit: 'submit',
  reset: 'reset'
})
